# Isaac Exim Configuration

This repository contains snippets of Exim 4 config files used by the Isaac projects, more as a reference resource than a backup. The directory structure aims to mimic that found in `/etc/exim4` in split configuration mode.
